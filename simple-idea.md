# Simple Idea

간단한 개선/리팩토링 아이디어는 이 파일에 `As-Is | To-Be` 두 열 표로 한 줄씩 누적한다.
복잡한 설계 논의, 실험 결과, 회고 기록은 `docs/Idea`, `docs/POC`, `docs/Retrospective`, `docs/ADR`로 분리한다.

| As-Is | To-Be |
|------|-------|
| ~~내 주문·예약·8회권은 20건씩 가져온 배열에서만 검색·상태 필터·정렬을 적용한다. 아직 가져오지 않은 거래는 검색 결과와 정렬에 포함되지 않는다.~~ | ~~검색·상태·정렬을 서버 조회에 전달하고 결과를 페이지로 나눈다. 세 목록의 조건 변경 시 커서도 초기화한다. [기능 검토 1번](docs/Idea/0040_추가_기능과_사용_흐름_개선_검토/idea.md)을 참고한다.~~ |
| ~~상품 검색어·유형·카테고리·정렬은 화면 상태에만 저장되어 목록 재진입·새로고침 시 초기화된다.~~ | ~~검색 조건을 URL에 저장하고 서버 첫 조회와 브라우저 조회에서 같은 조건을 사용한다. [기능 검토 2번](docs/Idea/0040_추가_기능과_사용_흐름_개선_검토/idea.md)을 참고한다.~~ |
| ~~스마트스토어 주문 수집이 실패하거나 덜 끝나도 별도 재고 전송이 실행되어 판매된 수량을 다시 올릴 수 있다.~~ | ~~자동·수동 재고 쓰기 직전에 주문 수집 완료를 확인하고 미반영 주문이 있는 원상품은 보류한다. 보류는 실패 횟수를 늘리지 않으며 관리자 재처리 뒤 최신 수량을 전송한다.~~ |
| ~~스마트스토어 주문 수집에서 재고 부족 예외를 잡아도 재고 서비스가 공유 트랜잭션을 롤백 대상으로 표시해 관리자 확인 기록이 유실된다.~~ | ~~단일 SKU 차감의 부족 결과를 재고 서비스 안에서 반환해 주문·확인 기록을 저장한다. 일반 주문의 일괄 차감은 기존 전체 롤백을 유지한다.~~ |
| ~~스마트스토어 수동 상품 반영이 자동 재고 전송보다 늦게 끝나면 이전 수량을 덮어쓰고 동기화 완료 상태가 남는다.~~ | ~~수동 외부 호출 종료 후 성공·부분 실패와 관계없이 기존 재고 동기화를 다시 요청한다. 실제 DB와 가짜 외부 응답으로 재고 10→9개 자동 반영 뒤 늦은 10개 전송을 다음 배치가 9개로 보정함을 확인한다.~~ |
| ~~미리보기는 상품 버전만 비교해 원상품·옵션 연결을 바꾸어도 기존 화면에서 다른 대상을 반영할 수 있다.~~ | ~~상품 정보·연결 행·원상품·옵션가·사용 여부를 포함한 `previewVersion`을 서버와 화면이 공유한다. 연결 변경·해제·재등록은 다시 비교하고 수량만 바뀌면 최신 수량을 사용한다.~~ |
| ~~Google OAuth `state`를 프론트 `sessionStorage`에서만 비교하고, 백엔드는 `code`/`redirectUri`만 받아 처리한다.~~ | ~~OAuth 시작 시 `state`를 서버 세션에도 저장하고, 콜백 또는 코드 교환 시 백엔드가 직접 검증해 login CSRF 방어 경계를 서버 쪽으로 옮긴다.~~ |
| ~~`AdminAuthFilter`가 URL 시작 문자열과 `/auth/` 포함 여부만 보고 관리자 경로를 판정한다.~~ | ~~공통 `RequestMatcher`로 공개 관리자 경로를 명시하고 Security 체인과 필터가 같은 규칙을 사용한다.~~ |
| ~~`AdminBookingResponse`는 비회원 예약 기준의 이름/전화번호 구조를 강하게 전제한다.~~ | ~~비회원 예약, 회원 예약, 이관된 예약을 모두 담을 수 있는 `customerSummary` 구조로 단순화한다.~~ |
| ~~`ProductQueryService`가 상품 목록 뒤에 재고를 건별 조회한다.~~ | ~~상품과 재고를 함께 읽는 조회 한 번으로 목록을 만들도록 바꾼다.~~ |
| ~~`Me*Controller` 3개가 DTO를 내부 record로 정의하고, 나머지는 별도 `dto/` 파일을 사용해 기준이 섞여 있다.~~ | ~~DTO는 항상 별도 파일(`dto/` 패키지)로 분리해 위치를 예측 가능하게 유지한다.~~ |
| ~~`@UseCaseIT` 일부가 실제 동작을 호출해 검증하는 부분에서도 구현 클래스(`Default*Service`, `*Service`)를 직접 주입한다. 그래서 웹 요청 검증과 서비스 직접 호출 검증이 한 테스트 안에 섞여 있다.~~ | ~~웹으로 들어오는 기능은 `MockMvc` 또는 유스케이스 인터페이스를 통해 호출하도록 맞춘다. 서비스 직접 주입은 테스트 데이터를 준비하거나 내부 동시성 경계를 직접 검증할 때만 남긴다.~~ |
| ~~컨트롤러가 호출하는 일부 `UseCase`가 하나의 요청 문맥을 이루는 값들을 낱개 인자로 직접 나열한다.~~ | ~~controller request와 별도로 앱 경계용 command DTO를 두어 유스케이스 입력을 의미 단위로 묶고 순서 의존을 줄인다.~~ |
| ~~주문 환불 흐름이 `Refund` 결과를 확인하지 않고 `ORDER_REFUNDED` 알림을 보낼 수 있다.~~ | ~~환불 완료 알림은 `RefundStatus.SUCCEEDED`일 때만 발송하고, 실패는 재시도 대상만 남긴다.~~ |
| ~~`Booking.reschedule()`와 `Booking.markNoShow()`의 상태 전이 검증이 서비스 레이어에 흩어져 있다.~~ | ~~예약 상태 전이 가능 여부를 도메인 메서드 안으로 옮겨 규칙을 한 곳에 응집시킨다.~~ |
| ~~`DefaultBookingCancelService`가 `BOOKED` 상태 확인을 직접 수행하고 `Booking.cancel()`은 단순 상태 대입만 한다.~~ | ~~취소 가능 상태 검증을 `Booking.cancel()` 안으로 옮겨 예약 취소 전이 규칙을 도메인에 응집시킨다.~~ |
| ~~`DefaultBookingCancelService`가 환불 가능 여부 판단과 8회권 크레딧 복구/예약금 환불 분기를 한 메서드에서 직접 조합한다.~~ | ~~예약 취소 후 보상 처리를 별도 준비 로직이나 전용 도우미로 묶어 시간 경계 판단과 보상 적용 책임을 분리한다.~~ |
| ~~예약금 예약 취소 시 `refundable`만 보고 `DEPOSIT_REFUNDED` 알림을 보내 PG 환불 실패와 성공을 구분하지 않는다.~~ | ~~`RefundStatus.SUCCEEDED`를 확인한 경우에만 환불 완료 알림을 보내고, 실패는 무알림 또는 별도 이벤트로 분리한다.~~ |
| UseCase가 JPA 엔티티(`Booking`, `Slot` 등)를 컨트롤러에 직접 반환한다. 현재는 즉시 DTO로 바꾸기 때문에 안전하지만, 비동기 처리 도입 시 `LazyInitializationException` 위험이 있다. | 비동기 응답 조립이 필요해지면 UseCase 반환 타입을 record로 바꾼다. `CancelResult`, `ProductionResult` 같은 기존 패턴을 따른다. |
| ~~Response DTO·UseCase record의 팩토리 메서드가 ID 값을 낱개 인자로 받는 곳이 섞여 있다. 필드 추가 시 컴파일 에러로 잡히지 않고 파라미터 순서 실수 여지가 있다.~~ | ~~현재 응답 팩토리는 엔티티나 결과 객체를 받도록 정리되어 있어 별도 변경 대상이 아니다. 인증 값 객체의 ID 팩토리는 이 범위에 포함하지 않는다.~~ |
| ~~다중 소셜 계정 확장 배포 호환성을 위해 `users.provider`, `users.provider_id`, 기존 복합 인덱스와 legacy 소셜 계정 승격 조회를 임시 유지한다.~~ | ~~모든 운영 인스턴스가 `user_social_accounts` 기반 버전으로 전환된 다음 릴리스에서 기존 컬럼·인덱스·legacy 조회 포트를 함께 제거한다.~~ |
| ~~롤링 배포 중 구 프런트 Google 콜백 호환을 위해 `state`가 없는 Google 코드 교환을 임시 허용한다.~~ | ~~호환 분기를 제거하고 Google과 Naver 모두 서버 `state`와 redirect URI 검증을 필수화한다.~~ |
| ~~소셜 provider ID가 처음이고 이메일이 기존 회원과 겹치면 계정 공유를 막기 위해 자동 연결하지 않는다.~~ | ~~로그인된 회원이 OAuth state를 다시 검증한 뒤 Google/Naver 계정을 명시적으로 연결할 수 있는 마이페이지 API와 화면을 추가한다.~~ |
| ~~Naver는 이메일 검증 상태를 제공하지 않지만 이메일 충돌이 없는 신규 회원은 현재 프로필 이메일로 생성한다.~~ | ~~신규 Naver 회원은 기준 이메일 없이 provider-scoped 계정으로 생성하고, 자체 검증 전에는 프로필 이메일을 로그인·복구 식별자로 사용하지 않는다.~~ |
| ~~프론트가 offset 없는 `LocalDateTime` 응답을 브라우저 현지 시간으로 해석해 사용자 시간대에 따라 표시 시각이 달라질 수 있다.~~ | ~~웹 클라이언트가 offset 없는 응답을 `Asia/Seoul`로 해석하고 offset이 있는 응답은 절대 시각으로 보존하도록 표시 정책을 통일한다.~~ |
| ~~고객 주문은 결제 후 조회만 제공하고 일반 취소 상태와 API가 없다.~~ | ~~`PAID_APPROVAL_PENDING`에서만 고객 취소를 허용하고, 관리자 `REJECTED`와 다른 상태·회원/비회원 소유권·재고 복구·이력·커밋 후 환불을 함께 구현한다.~~ |
| ~~관리자 8회권 환불은 환불할 `passId`를 직접 입력해야 하고 관리자용 8회권 검색·목록 API가 없다.~~ | ~~회원명·전화번호·8회권 번호로 조회한 목록에서 잔여 횟수와 환불 예상액을 확인한 뒤 환불하도록 관리자 조회 API와 목록 기반 액션을 추가한다.~~ |
| 비밀번호 해시는 롤백 호환을 위해 식별자 없는 BCrypt로 쓰고 식별자 없는 형식과 `{bcrypt}` 형식을 모두 읽는다. | 이 호환 릴리스가 운영과 롤백 기준선이 된 뒤 `{bcrypt}` 쓰기로 전환하고 기존 해시는 별도 migration으로 백필한다. |
| ~~29개 Spring Data repository가 애플리케이션 저장 포트의 구체 `save(Entity)`와 `JpaRepository`의 제네릭 `save(S)`를 한 인터페이스에서 함께 선언해 unchecked bridge 경고를 낸다.~~ | ~~저장 포트를 구현하는 전용 persistence adapter와 Spring Data repository를 분리해 제네릭 메서드 충돌을 없애고 컴파일 경고를 실제 신규 위험 신호로 사용할 수 있게 한다.~~ |
| ~~`BoundedExecutorFactory`의 수동 Micrometer 바인딩과 Spring Boot의 executor 자동 바인딩이 같은 meter를 등록해 기동 시 중복 경고가 발생한다.~~ | ~~executor meter 바인딩 책임을 한 곳으로 통합하고 직접 생성 테스트는 별도 binder 계약 테스트로 분리한다.~~ |
| ~~일부 Spring 통합 테스트 종료 시 Spring Session 정리 스레드가 먼저 닫힌 `LettuceConnectionFactory`를 한 번 더 참조해 오류 로그를 남긴다.~~ | ~~테스트 컨텍스트 종료 전에 세션 정리 스케줄러를 정지시키거나 테스트 프로필에서 해당 정리 작업을 비활성화해 실제 실패 로그와 종료 노이즈를 구분한다.~~ |
| ~~운영 공개 도메인이 정해지지 않아 절대 URL이 필요한 canonical, sitemap, `og:url`을 확정할 수 없다.~~ | ~~`https://happy-gallery.com`을 대표 origin으로 확정하고 SSR 공개 경로의 canonical·Open Graph·robots·sitemap을 한 origin으로 제공한다.~~ |
| ~~공개 후기 최신순·별점순 조회가 정렬 종류를 한 JPQL의 동적 `CASE` 표현식으로 선택하며, 운영 규모 데이터에서 MySQL 실행계획을 아직 확인하지 않았다.~~ | ~~운영 규모 표본의 `EXPLAIN ANALYZE`로 병목을 확인하고 대상·정렬별 고정 쿼리와 저평점 전용 인덱스로 분리했다.~~ |
| ~~관리자 신고 목록이 신고자 정보와 전체 증거 본문·사진을 각 목록 항목에 함께 반환해 목록 payload와 민감정보 노출 범위가 크다.~~ | ~~목록은 판단에 필요한 summary만 반환하고 신고자·전체 evidence는 Bearer 단건 상세 API로 분리했다.~~ |
| ~~숨김 이력이 없는 후기를 삭제·재작성할 때마다 비식별 tombstone 행이 누적되며 현재는 회원별 처리율 제한만 있다.~~ | ~~재활성화 시 과거 생명주기 식별자가 섞이는 문제를 피해, 증거 없는 비차단 tombstone을 30일 뒤 배치 파기한다.~~ |
| ~~스마트스토어 문의 조회가 실패하면 `SmartStoreInquirySection`이 오류만 반환해 상품 문의·주문/배송 문의 탭과 조회 조건도 사라진다.~~ | ~~탭과 조회 조건을 유지하고 목록·템플릿 오류 재시도를 제공한다. 이미 불러온 목록과 편집 중인 답변도 유지한다.~~ |
| ~~스마트스토어 주문·배송 문의 답변은 수정할 수 있지만 상품 문의는 답변을 표시하기만 한다. 서버는 이미 등록·수정 공용 PUT API를 호출한다.~~ | ~~상품 문의에 기존 본문 수정·저장·취소와 실패 시 초안 보존을 추가했다. [공식 답변 등록·수정 API](https://apicenter.commerce.naver.com/docs/commerce-api/current/create-or-update-answer-contents)를 재사용한다.~~ |
| ~~결제 실패 화면 `PaymentFailPage`는 홈·상품 목록 링크만 제공해 예약금이나 8회권 결제를 취소한 고객도 원래 구매 화면으로 돌아갈 수 없다.~~ | ~~같은 고객 세션의 내부 구매 경로로 돌아가는 버튼을 추가했다. 예약은 선택한 클래스에서 최신 시간을 다시 고르며 결제는 자동 재요청하지 않는다.~~ |
| ~~결제창을 닫고 구매 화면으로 돌아와도 서버 결제 시도는 `PENDING`이며 쿠폰·적립금 예약은 30분 만료 처리까지 남는다. 새 prepare는 같은 쿠폰이나 예약 중인 적립금을 사용할 수 없다.~~ | ~~본인 소유의 승인 전 결제를 종료하고 같은 트랜잭션에서 혜택 예약을 해제한다. 구매 화면 복귀·SDK 오류에 연결하며 승인 진행·대사·보상 상태는 변경하지 않는다.~~ |
| ~~영수증 링크는 결제 완료 화면과 비회원 결제 상태 화면에만 있고 회원 주문·예약·8회권 내역에는 없다.~~ | ~~회원 주문·예약 상세와 8회권 목록에서 현재 소유권을 확인한 뒤 기존 결제 이력의 영수증을 제공한다. 도메인에 값을 복제하지 않고 8회권은 일괄 조회한다.~~ |
| ~~스마트스토어 문의 조회는 서버에서 최근 30일로 고정하고 화면은 100건만 요청한다. 기간 선택과 다음 페이지가 없어 오래된 문의나 조회 한도를 넘는 답변 이력을 찾기 어렵다.~~ | ~~조회 기간과 50건 단위 페이지 이동을 추가했다. 조회 조건은 상품 문의·주문/배송 문의별로 유지하고 미답변 필터와 함께 사용한다.~~ |
| ~~`payment/flow.ts`의 0원 분기는 결제 복구 정보를 저장하기 전에 각 화면의 confirm을 호출한다. 주문서·장바구니·8회권 예약에서 confirm 응답이 유실되면 오류만 표시하고 다음 클릭은 새 prepare를 시작한다.~~ | ~~0원 결제도 현재 고객 세션에 확정 요청을 보관하고 공통 결과 화면에서 조회·재확인한다. 미확인 요청이 있으면 새 prepare 대신 기존 결과 화면으로 연결하며 화면별 confirm 콜백은 제거했다.~~ |
| ~~비회원 결제 준비는 인증코드를 소비하지만 `AuthGateModal`의 `guestVerified`와 코드가 그대로 남는다. 코드가 틀렸거나 결제창을 닫은 뒤 모달을 다시 열어도 인증 입력으로 돌아갈 수 없다. 비회원 주문서도 소비된 코드를 계속 제출할 수 있다.~~ | ~~prepare 성공과 인증 실패 때 코드만 초기화하고 재인증을 안내한다. 업무 오류에는 코드를 유지하며 상품·예약 선택과 이름을 보존한다. 재발송 때 상위 화면의 이전 코드도 해제한다.~~ |
| ~~상품 상세 `ProductDetailPage`의 회원 바로구매에는 쿠폰·적립금 선택과 요청 값이 없다. 같은 회원 주문인데 장바구니·주문서에서만 혜택을 적용할 수 있다.~~ | ~~기존 `MemberOrderBenefits`를 바로구매에도 재사용하고 세 주문 화면 모두 할인·배송비가 반영된 결제 예정 금액을 표시한다. 전액 할인은 공통 0원 결제 복구 경로를 공유하며 서버의 기존 혜택 계산·예약을 그대로 사용한다.~~ |
| ~~`PaymentSuccessPage`는 완료 응답을 받으면 확정 요청과 복귀 정보를 삭제하고 결과는 React 상태에만 둔다. URL도 정리하므로 완료 화면을 새로고침하면 정상 결제인데도 `결제 확정 실패` 화면이 나온다.~~ | ~~완료 URL에는 조회용 결제 번호만 남기고 현재 고객의 기존 상태 API로 내역과 영수증을 복원한다. 조회 오류는 결제 실패와 구분하고, confirm을 반복하거나 완료된 0원 결제로 다음 구매를 막지 않는다. 별도 결과 저장소는 추가하지 않았다.~~ |
| ~~`ProductPurchaseOptions`는 각인 문구가 다른 행의 재고를 각각 제한한다. 같은 옵션 조합 재고가 2개여도 문구 A 2개와 문구 B 2개를 선택할 수 있고, 바로구매는 서버 prepare에서 거절된다. 장바구니 담기는 행별 요청이어서 앞선 행만 추가될 수 있다.~~ | ~~같은 `productVariantId`의 수량을 합산해 옵션 추가·수량 변경·구매 버튼에 재고와 99개 한도를 적용하고 남은 수량을 표시한다. 행별 재고 사본은 제거하고 현재 상품 응답을 사용하며 서버의 최신 재고 검증은 유지한다.~~ |
| ~~비회원 옵션 주문을 넘겨받는 `OrderItemsForm`은 선택한 조합과 각인 입력을 요청에는 포함하지만 화면에는 상품명·수량·금액·고정 사양만 표시한다. 같은 상품의 다른 옵션이나 문구를 결제 직전에 구분하기 어렵다.~~ | ~~주문서에 옵션명·선택값·직접입력 문구와 추가금을 표시하고 고객 장바구니·주문 상세의 같은 표시 코드를 공통화했다. 서버 요청은 유지하며, 관리자 목록은 추가금을 생략하는 요약 표시이므로 변경하지 않았다.~~ |
| ~~상품 상세·비회원 장바구니·주문서는 직접입력 값을 구분자로 이어 항목 키를 만든다. 구분자가 들어간 한 문구와 여러 옵션 입력이 같은 키로 계산돼 다른 문구가 합쳐질 수 있다. 서버 `CartItem.lineKey`와 병합 요청 해시도 같은 종류의 문자열 결합을 사용한다.~~ | ~~프런트 키는 정렬된 배열의 JSON 직렬화를 공통으로 사용하고 서버는 Base64 인코딩으로 입력 구조를 구분한다. V162는 기존 항목 ID·수량을 유지하며 키만 전환한다. 정상 각인 문자는 제한하지 않는다.~~ |
| ~~`useGuestCart`의 추가·수량 변경은 행별 1~99개만 확인한다. 상세 화면에서 제한해도 장바구니에서 다시 늘리거나 재방문해 추가할 수 있고, 다른 각인 문구로 같은 조합 100개도 저장된다. 주문제작은 초과 저장 뒤 품절로 표시하며 기성품은 공개 응답에 실제 재고 수량도 없다.~~ | ~~비회원 추가·증가 시 최신 공개 재고와 기존 수량을 합산해 상품·조합별 재고 및 99개 한도를 적용한다. `stockQuantity`를 API 계약에 반영하고 재고 감소 시 수량 조정을 안내한다. 감소·삭제와 서버의 최종 재고 검증은 유지한다.~~ |
| ~~`ProductDetailPage`의 여러 옵션 담기는 항목마다 별도 요청을 보낸다. 앞 항목 저장 후 뒤 항목이 기존 장바구니 수량·재고 때문에 거절되면 일부만 담기며, 다시 누를 때 앞 항목이 중복 추가된다.~~ | ~~기존 다건 병합 API로 전체 선택을 저장하고 실패 후 같은 요청에는 같은 멱등키를 사용한다. 비회원도 전체 수량 확인 후 한 번만 저장하므로 실패 시 일부 항목이 남지 않는다.~~ |
| ~~회원 장바구니의 `DefaultCartService.updateItemQty`는 감소 요청에도 현재 옵션과 합산 재고를 검증한다. 5개를 담은 뒤 재고가 2개로 줄면 화면의 `−` 버튼은 4개를 요청하므로 실패하고, 계속 눌러도 수량을 줄이지 못한다. 판매 중지·옵션 변경도 감소를 막는다.~~ | ~~감소·유지는 소유권과 수량 범위만 확인하고 추가·증가에 현재 상품·옵션·합산 재고 검증을 적용한다. 구매 불가 항목은 수량 감소 또는 삭제를 안내하며 응답 필드는 늘리지 않았다.~~ |
| ~~`ProductPurchaseOptions`는 선택할 때 단가와 표시명을 `PurchaseLine`에 저장하고 `ProductDetailPage`는 이 단가로 합계를 계산한다. 장바구니 담기의 최신 상품 조회 등으로 기본가·옵션 추가금이 갱신되어도 기존 선택 행과 합계는 이전 가격을 표시하며, 서버 결제 준비는 현재 가격을 사용한다.~~ | ~~선택 행의 가격·표시명 사본을 제거하고 상품 상세·비회원 장바구니·주문서가 현재 상품 응답의 공통 계산을 사용한다. 가격·옵션 변경을 안내하고 유효하지 않은 선택은 삭제 후 재선택하도록 한다. 서버 가격 확정과 구매 당시 스냅샷은 유지한다.~~ |
| ~~서버 `CartItem.lineKey`는 직접입력의 옵션 키보다 관리자 표시 순서인 `sortOrder`를 먼저 정렬한다. 직접입력 두 그룹의 순서만 바꾸어도 같은 상품·조합·각인 문구가 다른 키가 되어 새 장바구니 행으로 담긴다. Java 25에서 실제 도메인 클래스로 키 불일치를 확인했다.~~ | ~~옵션 키 순으로 식별하고 V163으로 전환한다. 과거 중복 행의 ID·수량은 보존 키로 유지해 진행 중 결제를 보호하고 새 추가·병합은 입력이 같은 최소 ID에 합산한다. 직접입력은 잠금 조회에 함께 읽는다.~~ |
| ~~옵션 없는 주문제작 상품의 비회원 바로구매는 `OrderCreatePage`에서 `productVariantId: null`로 초기화한다. 서버는 기본 조합을 찾아 가격을 계산하지만 `productSelectionView`는 ID가 없으면 조합 추가금을 제외한다. 기본가 10,000원·기본 조합 추가금 2,000원 데이터로 실제 함수를 실행해 상세 12,000원·주문서 10,000원을 확인했다.~~ | ~~옵션 그룹이 없는 주문제작 상품만 현재 기본 조합으로 해석하고 같은 ID·단가를 표시와 결제 요청에 사용한다. 옵션 그룹이 있거나 명시한 조합이 사라졌으면 임의의 기본 조합으로 바꾸지 않는다.~~ |
| ~~`OrderItemsForm`은 현재 선택의 `configurationValid`와 재고를 결제 가능 여부에 반영하지 않는다. 판매 중지·조합 삭제 후에도 항목이 남고 `OrderCreatePage`는 상품 종류 조회만 끝나면 결제를 허용한다. 옵션 없는 상품은 상세 수량 입력과 주문서 추가도 실제 재고 대신 99개만 제한해 재고 2개 상품을 3개로 요청할 수 있으며, 서버 prepare에서 뒤늦게 거절된다.~~ | ~~수량 한도를 상세·주문서·비회원 장바구니에서 공통으로 사용하고 주문서는 하나의 조회 결과로 금액·상품 유형·구매 가능 여부를 계산한다. 구매 불가 항목은 결제를 막고 수량 감소·삭제·재선택을 제공하며 추가·증가에는 SKU 합산 재고를 적용한다. 서버 최종 검증은 유지한다.~~ |
| ~~비회원 옵션 주문 초안은 `hg_guest_order_draft` 한 곳에 저장된다. 다른 상품의 초안으로 덮인 뒤 이전 주문서로 돌아오거나 저장값 없이 `draft=options` URL에 접근하면 `readOptionDraft`가 null을 반환하고 옵션·각인 없는 기본 수량 1개로 대체된다. 필수 옵션은 결제 오류가 나고 선택 옵션만 있는 상품은 의도와 다른 기본 조합으로 결제될 수 있다.~~ | ~~초안을 주문 화면의 UUID별로 저장하고 기존 고객 세션 소유자 확인을 재사용한다. 복원 실패는 재선택을 안내하고 기본 주문으로 대체하지 않는다. 저장 실패 시 현재 선택을 유지하며 이전 단일 저장소의 초안은 임의로 가져오지 않는다.~~ |
| ~~`OrderCreatePage`는 최초 초안만 읽고 주문서의 수량 변경·항목 삭제·상품 추가는 `setItems`에만 반영한다. 새로고침하거나 결제 실패 화면의 원래 주문서 링크로 돌아오면 수정 전 초안·URL 수량이 복원된다.~~ | ~~일반·수동 주문서에도 초안 번호를 붙이고 기존 고객 세션 소유자·진입 상품을 유지해 변경 이벤트에서 저장한다. 여러 상품과 빈 품목을 복원하며 인증 정보는 저장하지 않는다. 저장 실패는 현재 입력을 유지하고 재저장 전 결제를 막는다.~~ |
| ~~`ProductPurchaseOptions`의 `selectedValues`는 삭제된 선택형 옵션 그룹의 키도 유지한다. 색상·크기를 고른 뒤 관리자가 색상 그룹을 삭제하면 화면에는 크기만 남지만 `matchesVariant`는 숨은 색상까지 비교해 판매 중인 크기 조합도 찾지 못한다. 기존 컴포넌트 실행으로 추가 실패를 확인했다.~~ | ~~현재 선택형 그룹·값에 속하는 선택만 조합 찾기·필수 선택 확인·입력 표시에서 사용한다. 이미 선택한 품목의 옵션 변경 안내는 유지하며 별도 초기화 effect 없이 유효한 옵션을 다시 추가할 수 있다.~~ |
| ~~`useGuestCart`의 추가·증가는 최신 상품을 읽어도 `requireStock`에서 수량만 확인한다. 각인 그룹이 삭제되고 기본 SKU는 판매 중인 데이터로 실행하면 이전 각인 항목의 추가와 1→2개 증가가 성공하고, 조회에서는 즉시 구매 불가로 표시된다.~~ | ~~추가·증가 대상 각 항목에 기존 `productSelectionView` 결과를 적용하고 재고는 SKU별로 합산한다. 하나라도 유효하지 않으면 저장 전에 실패하며, 구매 불가 옵션의 증가 버튼은 막되 감소·삭제는 허용한다.~~ |
| ~~기준 PRD §3.1은 픽업 만료 시 자동 환불하지 않는다고 명시하지만 §3.3에는 기성품 미수령 자동 환불 문구가 남아 있다. `PickupExpireProcessor`와 ADR-0013은 기성품 재고만 복구하고 `PICKUP_FORFEITED`로 종료한다.~~ | ~~§3.3의 자동환불 문구를 제거하고 종료·재고 복구·관리자 예외 환불은 §3.1 정책을 참조하도록 했다. 운영 코드의 환불 동작은 변경하지 않았다.~~ |
| ~~주문제작 상품 편집은 화면을 열 때 읽은 조합 재고를 상품명·설명만 바꿔도 다시 제출한다. `ProductOptionConfigurationService.syncVariants`는 잠금 조회한 최신 재고를 요청 수량으로 덮어쓴다. 실제 서비스 메서드 실행에서 재고 5개 초안을 보관한 뒤 1개를 판매하고 초안을 저장하자 4개가 5개로 복구됐다.~~ | ~~기존 조합의 정보 수정 메서드에서 수량 인자를 제거해 현재 재고를 보존한다. 기존 재고는 이력·스마트스토어 동기화가 있는 재고 조정 기능에서만 바꾸며 신규 조합만 최초 재고를 받는다.~~ |
| ~~선택형 옵션이 없는 주문제작 상품은 `ProductEditModal`의 기본 조합 재고 입력값과 기존 `variants`를 함께 제출한다. 서버는 `variants`가 있으면 별도 `quantity`를 사용하지 않는다. 실제 서비스 메서드에 기본 재고 9개와 기존 조합 5개를 전달하자 저장 결과가 5개였다.~~ | ~~기본·선택 조합의 기존 재고는 읽기 전용으로 표시하고 상품 목록의 재고 조정을 안내한다. 신규 기본 조합에만 별도 최초 재고를 제출하며 직접입력 옵션 수정 시 기존 기본 조합 설정을 유지한다.~~ |
| ~~관리자 상품 수정 API에서 선택형 그룹의 `sortOrder`를 바꾸면 조합 식별 문자열도 바뀐다. 실제 키 생성 메서드에서 같은 선택이 `color=red\|size=large`와 `size=large\|color=red`로 달라졌다. `syncVariants`는 새 조합을 만들고 기존 조합을 비활성화하므로 기존 장바구니의 조합 번호로 구매할 수 없어진다.~~ | ~~조합 키를 옵션 키 순으로 고정하고 V164에서 번호·재고·참조를 유지하며 변환한다. 과거 중복 조합은 활성·최소 ID 우선으로 현재 조합을 정하고 나머지는 비활성 상태로 보존하며 현재 편집 목록에서 제외한다.~~ |
| ~~`ProductOptionConfigurationService.toOptions`는 선택값이 없는 과거 조합도 관리자 응답에 포함한다. 실제 `configure` 메서드로 기본 조합 상품에 선택형 옵션을 추가하자 현재 미선택 조합과 비활성 `DEFAULT`가 함께 반환됐고, 응답 그대로 재저장하면 "같은 옵션 조합이 중복되었습니다"로 실패했다.~~ | ~~현재 옵션 그룹으로 만들 수 있는 조합 키에 해당하는 행만 편집 대상으로 반환한다. 판매를 잠시 중지한 현재 조합은 유지하고, 구조 변경으로 사라진 조합은 과거 주문 참조용으로만 남긴다. 기본 조합에서 선택 옵션 추가, 필수 여부 변경, 그룹 삭제 후 재저장을 확인한다.~~ |
| ~~`SmartStoreInventoryModal`은 관리자 상품 응답에 보이는 조합만 제출하지만 `DefaultSmartStoreInventoryService.validate`와 `SmartStoreStockSyncTransactionService`는 과거 조합까지 전부 요구한다. 실제 옵션 교체 후 화면 대상 3개·DB 대상 4개로 연동 검증을 실행하자 모든 조합을 매핑하라는 오류가 발생했다. V164의 숨겨진 과거 중복 조합도 같은 불일치가 생긴다.~~ | ~~현재 조합의 매핑 저장·재고 전송 범위를 편집 응답과 맞춘다. 과거 조합의 기존 원격 매핑은 판매 가능 재고가 남지 않도록 0개 전송·해제 절차를 구분하고, 숨겨진 조합의 신규 매핑을 관리자에게 요구하지 않는다. 과거 주문의 조합 번호와 재고 복구 경로는 유지한다.~~ |
| ~~`admin-content-recovery.spec.ts` 공지 테스트는 처리하지 않은 관리자 요청에 일괄 `[]`을 반환한다. 새 스마트스토어 문의·상품 공지 API의 객체 응답과 달라 화면이 예외 처리 화면으로 바뀐다. 브라우저에서 문의 `{content: []}`와 상품 공지 `{notices: []}` 응답으로 바꾸자 관리자 제목과 공지 수정 버튼이 표시됐다.~~ | ~~관리자 문의·상품 공지·회계 응답을 생성 타입에 맞추고 고객 인증·공방 조회를 별도로 준비한다. 정의하지 않은 관리자 요청은 즉시 실패하도록 바꾸고 현재 로그아웃 안내 문구를 확인한다. 운영 방어 코드는 추가하지 않았으며 공지·설정·주문 E2E 3건이 통과했다.~~ |
| ~~같은 현재 조합의 스마트스토어 옵션 번호를 바꾸면 이전 매핑이 삭제되어 이전 옵션의 재고 0개 전송이 빠진다. 네이버 부분 수정 API는 요청에서 빠진 정보를 보존하므로 이전 판매 재고가 남을 수 있다.~~ | ~~V166의 `retired`로 같은 SKU의 과거 연결을 보존하고 현재 재고와 함께 0개를 전송·재시도한다. 매핑 응답에는 현재 연결만 반환하고 미리보기는 원격 옵션 번호로 행을 구분한다. 기존 데이터 전환, 연결 재변경·원격 번호 재사용, 재고 복구 후에도 과거 옵션은 0개인 흐름을 MySQL 테스트로 확인했다. 이미 삭제된 연결은 자동 복원하지 않는다.~~ |
| ~~재고 전송 중 연동을 비활성화하면 동기화 행을 삭제하고, 재활성화 시 요청 버전 1로 새 행을 만들어 이전 1번 요청의 성공 응답이 새 요청을 완료 처리한다.~~ | ~~V165로 동기화 행 생성 식별자를 추가하고 Java UUID를 선점·완료·실패에 함께 전달한다. MySQL 통합 테스트로 비활성화→재활성화와 해제→재등록 뒤 이전 성공·실패 응답이 새 요청의 상태·시도 횟수·오류·재시도 시각을 바꾸지 않고 새 전송이 완료됨을 확인했다.~~ |
