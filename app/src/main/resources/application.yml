spring:
  application:
    name: happygallery
  profiles:
    default: local
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  admin:
    api-key: ${ADMIN_API_KEY:dev-admin-key}
